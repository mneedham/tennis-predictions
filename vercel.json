{
    "builds": [
        {
            "src": "api/test.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/api/test/(.*)",
            "dst": "api/test.py"
        }
    ]
}